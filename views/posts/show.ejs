<!DOCTYPE html>
<html lang="en">
    <%- include("../partials/head") %>
<body>
    <main>
        <h1><%= post.title %></h1>
        <p><%= post.text %></p>
        <p>Created by: <%= post.author.username %> at <%= post.createdAt %></p>
        
        <form action="/posts/<%= post.id %>?_method=DELETE" method="post">
            <input type="submit" value="Delete">
        </form>
        <a href="/posts">Back</a>
        <a href="/posts/<%= post.id %>/edit">Edit</a>
    </main>
    <section>
        <h2>Comments:</h2>
        <% for (comment of post.comments) { %>
            <a href="/comments/<%=comment.id%>"><p><%= comment.text %></p></a>
        <% } %>
        <h3>Add comment:</h3>
        <form action="/comments/<%= post.id%>" method="post">
            <input type="textarea" name="text" placeholder="What do you want to say?"><br />
            <input type="submit" value="Submit"><br />
        </form>
    </section>
</body>
</html>